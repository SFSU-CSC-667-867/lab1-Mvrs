// Arrays in js

// arrays can be delared explicitly
const array = [9, 3, 4];

// or with new keyword
const anotherArray = new Array(5);

// access array elements
console.log(array[0]);

// change elements
array[1] = 'hello';

const newArr = []

console.log(array)

// for practice


// use forEach
array.forEach( num => {
    if (num === NaN) {
        
    }
    newArr.push(num * 2)
})

// console.log(array)
console.log(newArr)
// use map

// use pop

// use push

// use shift

// use unshift

// use filter